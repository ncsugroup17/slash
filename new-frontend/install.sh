#!/bin/bash

# Run this script to install dependencies with the legacy peer dependencies flag
npm install --legacy-peer-deps

echo "Dependencies installed successfully with legacy peer dependencies flag."
echo "Run 'npm run dev' to start the development server." 